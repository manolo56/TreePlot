# TreePlot
A class to plot Tree objects (from BioPython Phylo module) using matplotlib. 

Internally, it uses a dataframe representation of the tree similar to the ape package in R. 

The TreePlot class is a wrapper around this representation and other graphical parameters. 

Example usages can be found in the demonstration notebook. 
